<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<!-- 어노테이션과 병행할경우 namespace를 dao(mapper.java)인터페이스 명칭과 맞춰야 함 -->
<mapper namespace="kr.spring.cart.dao.CartMapper">  
	<select id="selectRowCount" parameterType="map" resultType="integer">
		select 
			count(*)
		FROM cart c LEFT OUTER 
  		            JOIN product p
  		            ON c.product_num = p.product_num 
		<where>
			mem_num = #{mem_num} 
			<if test="keyword != null and keyword !=''">
				and title like '%' || #{keyword} || '%'
			</if>
			<if test="keyfield != null and keyfield !=''">
				<if test="keyfield == 1">
					and status = 1
				</if>
				<if test="keyfield == 2">
					and status = 2
				</if> 
			</if>
		</where>
	</select>
	
	
	<select id="selectListCart" parameterType="map" resultType="cartVO">
  		SELECT
  		   *
  		FROM (SELECT
  		        a.*,
  		        rownum rnum
  		      FROM (SELECT 
  		               *
  		            FROM cart LEFT OUTER 
  		            JOIN product
  		            USING(product_num)
  		            <where>
						cart.mem_num = #{mem_num} 
					<if test="keyword != null and keyword !=''">
						and product.title like '%' || #{keyword} || '%'
					</if>
					<if test="keyfield != null and keyfield !=''">
						<if test="keyfield == 1">
							and cart.status = 1
						</if>
						<if test="keyfield == 2">
							and cart.status = 2
						</if> 
					</if>
					</where>
  		            ORDER BY cart.reg_date DESC)a) 
  	<![CDATA[
  	WHERE rnum >= #{start} AND rnum <= #{end}
  	]]>	            
  	</select>
</mapper>























